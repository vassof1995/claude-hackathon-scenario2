# Copy to `.env` (git-ignored) and fill in. NEVER commit real secrets.
# In AWS these are AWS Secrets Manager / SSM Parameter Store references, not literals.
POSTGRES_PASSWORD=change-me-locally
MINIO_ROOT_USER=contoso
MINIO_ROOT_PASSWORD=change-me-locally
